# 统一换行符:仓库内存 LF,工作区按需(text=auto 避免一次性 renorm)
* text=auto

# Shell 脚本必须 LF(Windows / git bash 下 CRLF 会破坏 #!/usr/bin/env bash)
*.sh text eol=lf

# 二进制兜底
*.png binary
*.jpg binary
*.pdf binary
*.zip binary
