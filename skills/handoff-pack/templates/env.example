# .env.example — 只含键名,不含真实值 / keys only, NO real secrets.
# 复制为 .env 并填值;真实 .env 必须在 .gitignore 中,绝不提交。
# Copy to .env and fill in; the real .env must be git-ignored, never committed.

# --- 示例 / examples (按项目替换 / replace per project) ---
# API_KEY=
# DATABASE_URL=
# PORT=3000
# LOG_LEVEL=info
